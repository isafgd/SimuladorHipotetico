#Run Dockerfile in maven
$ mvn package docker:build

#Run Dockerfile in terminal
$ docker build -t trabalhops/simulador_hipotetico:1.0-SNAPSHOT .

> Transform an image in a container (Port 8080)
$ docker run -d -p 8080:8080 trabalhops/simulador_hipotetico:1.0-SNAPSHOT


> Remove api container
$ docker rm -f $( docker ps --filter "name=simulador_hipotetico" -a -q )